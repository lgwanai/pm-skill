---
phase: 02-knowledge-compilation-retrieval
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [tests/conftest.py, tests/test_compiler.py, tests/test_search.py]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Tests can run with pytest"
    - "Fixtures provide mock LLM responses"
    - "Fixtures provide sample wiki structures"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Shared fixtures for compiler and search tests"
      exports: ["mock_anthropic_response", "sample_raw_markdown", "sample_wiki_page"]
    - path: "tests/test_compiler.py"
      provides: "Compilation unit tests"
      contains: "test_"
    - path: "tests/test_search.py"
      provides: "Search functionality tests"
      contains: "test_"
  key_links:
    - from: "tests/conftest.py"
      to: "tests/test_compiler.py"
      via: "pytest fixtures"
      pattern: "def mock_anthropic_response"
---

<objective>
Set up test infrastructure for Phase 2 knowledge compilation and retrieval features.

Purpose: Wave 0 infrastructure enables TDD for compiler and search modules.
Output: Test files with fixtures and test scaffolds.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
From scripts/config.py:
```python
class LLMConfig(BaseModel):
    model: str = "claude-sonnet-4-6-20250528"
    api_key_env: str = "ANTHROPIC_API_KEY"

class PMSkillConfig(BaseModel):
    raw_dir: Path
    wiki_dir: Path
    log_dir: Path
    llm: LLMConfig
```

From scripts/cli.py:
```python
app = typer.Typer(name="pm-skill")
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create test fixtures in conftest.py</name>
  <files>tests/conftest.py</files>
  <action>
    Create shared fixtures for Phase 2 testing:
    
    1. `mock_anthropic_response` fixture - returns a mock Anthropic API response with structured wiki content including Overview, Entities, Concepts sections with confidence annotations.
    
    2. `sample_raw_markdown` fixture - provides a sample raw markdown document for compilation testing.
    
    3. `sample_wiki_page` fixture - provides expected wiki page structure for validation.
    
    4. `temp_wiki_dir` fixture - creates a temporary wiki/ directory structure for integration tests.
    
    5. `mock_config` fixture - provides a mock PMSkillConfig with test settings.
    
    Use pytest fixtures with appropriate scope (function for most, module for expensive setup).
  </action>
  <verify>
    <automated>pytest tests/conftest.py -v --collect-only 2>&1 | grep -q "conftest.py"</automated>
  </verify>
  <done>conftest.py exists with all 5 fixtures defined and importable</done>
</task>

<task type="auto">
  <name>Task 2: Create compiler test scaffolds</name>
  <files>tests/test_compiler.py</files>
  <action>
    Create test scaffolds for compiler module with placeholder tests:
    
    1. `test_compile_single_file` - test compiling one raw file to wiki entity page
    2. `test_compile_with_hash_check` - test that unchanged files are skipped
    3. `test_compile_force_flag` - test force recompilation
    4. `test_entity_page_structure` - verify entity page has required sections (Overview, Key Content, Entities, Concepts, Relations)
    5. `test_confidence_annotations` - verify confidence annotations are present (EXTRACTED/INFERRED/AMBIGUOUS/UNVERIFIED)
    6. `test_concept_extraction` - test concept page generation
    7. `test_index_generation` - test wiki/index.md creation
    8. `test_glossary_generation` - test wiki/glossary.md creation
    9. `test_log_writing` - test compilation log in log/ directory
    
    Each test should be a pytest function with `pass` body and appropriate docstring. Use fixtures from conftest.py.
  </action>
  <verify>
    <automated>pytest tests/test_compiler.py --collect-only -q 2>&1 | grep -c "test_" | grep -q "9"</automated>
  </verify>
  <done>test_compiler.py exists with 9 test function stubs</done>
</task>

<task type="auto">
  <name>Task 3: Create search test scaffolds</name>
  <files>tests/test_search.py</files>
  <action>
    Create test scaffolds for search module with placeholder tests:
    
    1. `test_fts5_search_basic` - test basic FTS5 keyword search
    2. `test_fts5_search_with_scope` - test search limited to entities or concepts
    3. `test_fts5_search_with_limit` - test result limiting
    4. `test_search_context_lines` - test --context flag showing surrounding lines
    5. `test_search_format_json` - test JSON output format
    6. `test_list_entities` - test listing all entity pages
    7. `test_list_concepts` - test listing all concept pages
    8. `test_list_with_format` - test list output formats
    
    Each test should be a pytest function with `pass` body and appropriate docstring.
  </action>
  <verify>
    <automated>pytest tests/test_search.py --collect-only -q 2>&1 | grep -c "test_" | grep -q "8"</automated>
  </verify>
  <done>test_search.py exists with 8 test function stubs</done>
</task>

</tasks>

<verification>
- All test files exist in tests/
- pytest can discover all test functions
- Fixtures are importable from conftest.py
</verification>

<success_criteria>
- tests/conftest.py with 5 fixtures
- tests/test_compiler.py with 9 test stubs
- tests/test_search.py with 8 test stubs
- pytest --collect-only succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-knowledge-compilation-retrieval/02-01-SUMMARY.md`
</output>
