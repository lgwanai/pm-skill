---
phase: 02-knowledge-compilation-retrieval
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified: [scripts/cli.py, scripts/search.py]
autonomous: true
requirements: [RET-01, RET-02, RET-03]
user_setup: []

must_haves:
  truths:
    - "User can search wiki pages and see matching results with context"
    - "User can list all entity pages with titles"
    - "User can list all concept pages with titles"
    - "Search supports different output formats (text, json, table)"
  artifacts:
    - path: "scripts/search.py"
      provides: "Search and list functionality"
      exports: ["search_wiki", "list_wiki_pages", "format_search_results"]
    - path: "scripts/cli.py"
      provides: "CLI commands for search and list"
      contains: "def search|def list"
  key_links:
    - from: "scripts/cli.py"
      to: "scripts/search.py"
      via: "search and list commands"
      pattern: "search_wiki|list_wiki_pages"
---

<objective>
Implement CLI search and list commands for wiki knowledge retrieval.

Purpose: Enable users to search compiled wiki and list available pages via CLI.
Output: search and list commands with FTS5 and ripgrep backends.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
From scripts/search.py (Plan 02):
```python
def init_fts5_table(db_path: Path) -> None
def index_wiki_page(db_path: Path, path: str, title: str, content: str, page_type: str) -> None
def search_wiki(db_path: Path, query: str, scope: str = None, limit: int = 20) -> list[dict]
```

From scripts/cli.py:
```python
app = typer.Typer(name="pm-skill")
console = Console()
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement search command</name>
  <files>scripts/cli.py, scripts/search.py</files>
  <behavior>
    - Test: search returns matching results with context
    - Test: --scope limits search to entities or concepts
    - Test: --limit caps number of results
    - Test: --format json outputs valid JSON
  </behavior>
  <action>
    1. Add `search` command to cli.py:
       - `pm-skill search <query>` - search wiki pages
       - Options: --context (default 3), --format (text/json/table), --scope (entities/concepts/all), --limit (default 20)
       - Display results with context lines using Rich formatting
    
    2. Extend search.py with:
       - `format_search_results(results: list, context_lines: int, format: str) -> str` - format output
       - `get_page_context(page_path: Path, match_line: int, context: int) -> list[str]` - get surrounding lines
    
    Use FTS5 search from Plan 02 as primary backend. Fall back to ripgrep for raw/ searches if needed.
  </action>
  <verify>
    <automated>pytest tests/test_search.py::test_fts5_search_basic tests/test_search.py::test_search_context_lines tests/test_search.py::test_search_format_json -x</automated>
  </verify>
  <done>search command works with all options, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement list command</name>
  <files>scripts/cli.py, scripts/search.py</files>
  <behavior>
    - Test: list shows all entity pages with titles
    - Test: --scope concepts lists concept pages
    - Test: --format json outputs valid JSON
  </behavior>
  <action>
    1. Add `list` command to cli.py:
       - `pm-skill list` - list wiki pages
       - Options: --scope (entities/concepts/all), --format (text/json)
       - Display file names with titles extracted from frontmatter or first heading
    
    2. Extend search.py with:
       - `list_wiki_pages(wiki_dir: Path, scope: str = "all") -> list[dict]` - list pages with metadata
       - `extract_title(page_path: Path) -> str` - get title from frontmatter or first H1
    
    Follow existing CLI patterns from import command.
  </action>
  <verify>
    <automated>pytest tests/test_search.py::test_list_entities tests/test_search.py::test_list_concepts tests/test_search.py::test_list_with_format -x</automated>
  </verify>
  <done>list command works with all options, tests pass</done>
</task>

</tasks>

<verification>
- pm-skill search --help shows command with all options
- pm-skill list --help shows command with all options
- Search returns results with context lines
- List shows page titles correctly
</verification>

<success_criteria>
- User can run `pm-skill search <query>` and see matching wiki pages with context
- User can run `pm-skill list --scope entities` and see all entity pages
- Search results include file path, title, and matching context
- JSON output format available for both commands
</success_criteria>

<output>
After completion, create `.planning/phases/02-knowledge-compilation-retrieval/02-03-SUMMARY.md`
</output>
