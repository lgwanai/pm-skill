---
phase: 02-knowledge-compilation-retrieval
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified: [scripts/compiler.py, scripts/search.py, references/prompts/compile.md, scripts/cli.py, scripts/utils/init.py]
autonomous: true
requirements: [CMP-01, CMP-02, CMP-03, CMP-04, CMP-05, CMP-06, CMP-07, CMP-08, RET-04]
user_setup:
  - service: anthropic
    why: "LLM-powered knowledge compilation"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys"

must_haves:
  truths:
    - "User can run compile command and see raw files transformed to wiki"
    - "Each raw file generates an entity page in wiki/entities/"
    - "Entity pages have required sections (Overview, Key Content, Entities, Concepts, Relations)"
    - "Confidence annotations present in compiled output"
    - "FTS5 index populated with wiki content"
  artifacts:
    - path: "scripts/compiler.py"
      provides: "LLM-powered compilation logic"
      exports: ["compile_document", "write_entity_page", "update_index"]
    - path: "scripts/search.py"
      provides: "FTS5 search functionality"
      exports: ["init_fts5_table", "index_wiki_page", "search_wiki"]
    - path: "references/prompts/compile.md"
      provides: "Compilation prompt template"
      contains: "EXTRACTED"
  key_links:
    - from: "scripts/cli.py"
      to: "scripts/compiler.py"
      via: "compile command"
      pattern: "def compile"
    - from: "scripts/compiler.py"
      to: "scripts/search.py"
      via: "FTS5 indexing after write"
      pattern: "index_wiki_page"
---

<objective>
Implement the compile command that transforms raw Markdown documents into structured wiki knowledge using LLM-powered compilation.

Purpose: Core knowledge compilation feature enabling raw→wiki transformation with confidence annotations.
Output: compiler.py module, compile CLI command, FTS5 search index.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-knowledge-compilation-retrieval/02-CONTEXT.md
</context>

<interfaces>
From scripts/config.py:
```python
class LLMConfig(BaseModel):
    model: str = "claude-sonnet-4-6-20250528"
    api_key_env: str = "ANTHROPIC_API_KEY"
```

From scripts/cli.py (existing pattern):
```python
app = typer.Typer(name="pm-skill")
console = Console()
```

From scripts/importer.py:
```python
# Uses markitdown for document conversion
# Stores content_hash in documents table
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create compilation prompt template</name>
  <files>references/prompts/compile.md, scripts/compiler.py</files>
  <behavior>
    - Test: Prompt template loads and contains required sections
    - Test: Template includes confidence annotation instructions
  </behavior>
  <action>
    1. Create `references/prompts/compile.md` with LLM Wiki methodology prompt:
       - Instructions for transforming document into structured wiki format
       - Required output sections: Overview, Key Content, Entities, Concepts, Relations
       - Confidence annotation format: EXTRACTED, INFERRED, AMBIGUOUS, UNVERIFIED
       - Example input/output for few-shot guidance
    
    2. In `scripts/compiler.py`, create `load_compile_prompt()` function that reads the prompt template.
    
    Use the exact structure from CONTEXT.md decisions section.
  </action>
  <verify>
    <automated>test -f references/prompts/compile.md && grep -q "EXTRACTED" references/prompts/compile.md</automated>
  </verify>
  <done>Prompt template exists with confidence annotation instructions</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement LLM compilation core</name>
  <files>scripts/compiler.py</files>
  <behavior>
    - Test: compile_document returns structured wiki content
    - Test: Confidence annotations present in output
    - Test: Hash check prevents recompilation of unchanged files
  </behavior>
  <action>
    Implement `scripts/compiler.py` with:
    
    1. `compile_document(content: str, config: PMSkillConfig) -> dict` - calls Anthropic API with compile prompt, returns structured dict with: overview, key_content, entities, concepts, relations, confidence_annotations.
    
    2. `compute_content_hash(content: str) -> str` - SHA-256 hash for change detection.
    
    3. `check_hash_changed(db_path: Path, file_path: Path, content: str) -> bool` - compare stored hash with current content.
    
    4. `write_entity_page(wiki_dir: Path, name: str, compiled: dict) -> Path` - write wiki/entities/{name}.md with structured sections.
    
    Use anthropic SDK directly (not LangChain) per user decision. Read API key from `config.llm.api_key_env` environment variable.
  </action>
  <verify>
    <automated>pytest tests/test_compiler.py::test_compile_single_file -x</automated>
  </verify>
  <done>compiler.py implements all 4 core functions, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement wiki structure generation</name>
  <files>scripts/compiler.py, scripts/utils/init.py</files>
  <behavior>
    - Test: Concept pages generated in wiki/concepts/
    - Test: wiki/index.md created with knowledge map
    - Test: wiki/glossary.md created with terms
  </behavior>
  <action>
    Extend compiler.py with:
    
    1. `extract_concepts(compiled: dict) -> list[str]` - extract concept names from compiled output.
    
    2. `write_concept_page(wiki_dir: Path, concept: str, sources: list) -> Path` - create wiki/concepts/{concept}.md with backlinks.
    
    3. `update_index(wiki_dir: Path, entities: list, concepts: list) -> None` - update wiki/index.md with navigable knowledge map.
    
    4. `update_glossary(wiki_dir: Path, terms: dict) -> None` - update wiki/glossary.md with terminology definitions.
    
    5. `write_compile_log(log_dir: Path, summary: dict) -> None` - write compilation summary to log/ directory.
    
    Ensure wiki/ directory structure exists (extend init.py if needed).
  </action>
  <verify>
    <automated>pytest tests/test_compiler.py::test_concept_extraction tests/test_compiler.py::test_index_generation tests/test_compiler.py::test_glossary_generation -x</automated>
  </verify>
  <done>All wiki structure generation functions implemented, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add compile CLI command and FTS5 index</name>
  <files>scripts/cli.py, scripts/search.py</files>
  <behavior>
    - Test: compile command processes raw files
    - Test: FTS5 index updated after compile
    - Test: --force flag forces recompilation
  </behavior>
  <action>
    1. Add `compile` command to cli.py:
       - `pm-skill compile` - compile all files in raw/
       - Options: --file, --force, --dry-run, --model
       - Show progress with Rich, display summary counts
    
    2. Create `scripts/search.py` with FTS5 functions:
       - `init_fts5_table(db_path: Path) -> None` - create wiki_search virtual table
       - `index_wiki_page(db_path: Path, path: str, title: str, content: str, page_type: str) -> None` - add/update page in FTS5
       - `search_wiki(db_path: Path, query: str, scope: str = None, limit: int = 20) -> list[dict]` - FTS5 search with bm25 ranking
    
    3. Integrate FTS5 indexing into compile workflow - after each entity page is written, index it.
    
    Follow existing CLI patterns from cli.py (Typer, Rich console).
  </action>
  <verify>
    <automated>pytest tests/test_compiler.py::test_compile_force_flag tests/test_search.py::test_fts5_search_basic -x</automated>
  </verify>
  <done>compile command works, FTS5 table created, pages indexed after compile</done>
</task>

</tasks>

<verification>
- pm-skill compile --help shows command
- Compile creates wiki/entities/ pages from raw/
- Each entity page has required sections with confidence annotations
- FTS5 table exists with indexed content
</verification>

<success_criteria>
- User can run `pm-skill compile` and see raw files transformed
- Each entity page has Overview, Key Content, Entities, Concepts, Relations sections
- Confidence annotations present in compiled output (EXTRACTED/INFERRED/AMBIGUOUS/UNVERIFIED)
- FTS5 wiki_search table exists and is populated
</success_criteria>

<output>
After completion, create `.planning/phases/02-knowledge-compilation-retrieval/02-02-SUMMARY.md`
</output>
