---
phase: 03-prd-generation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [tests/test_prd.py, tests/conftest.py, references/templates/toc-prd.md.jinja2, references/templates/tob-prd.md.jinja2, references/templates/backend-prd.md.jinja2, references/templates/miniprogram-prd.md.jinja2, references/prompts/collect.md, references/prompts/suggest.md, references/prompts/assess.md, references/prompts/compare.md]
autonomous: true
requirements: [PRD-01, PRD-02, PRD-03, PRD-04, PRD-05, PRD-06, PRD-07, PRD-08, PRD-09, PRD-10, PRD-11]
user_setup: []

must_haves:
  truths:
    - "Test scaffolds exist for all PRD requirements"
    - "All 4 PRD template files exist in references/templates/"
    - "All prompt templates exist in references/prompts/"
  artifacts:
    - path: "tests/test_prd.py"
      provides: "Test scaffolds for PRD module"
      contains: "TestConversation, TestSuggestion, TestRetrieval, TestIterativeRetrieval, TestConflictDetection, TestRiskAssessment, TestPRDStructure, TestTemplates"
    - path: "references/templates/toc-prd.md.jinja2"
      provides: "ToC product PRD template"
      min_lines: 30
    - path: "references/templates/tob-prd.md.jinja2"
      provides: "ToB product PRD template"
      min_lines: 30
    - path: "references/templates/backend-prd.md.jinja2"
      provides: "Backend system PRD template"
      min_lines: 30
    - path: "references/templates/miniprogram-prd.md.jinja2"
      provides: "Mini-program PRD template"
      min_lines: 30
    - path: "references/prompts/collect.md"
      provides: "Requirement collection prompt"
      contains: "multi-round, requirement"
    - path: "references/prompts/suggest.md"
      provides: "Keyword suggestion prompt"
      contains: "keyword, concept"
    - path: "references/prompts/assess.md"
      provides: "Risk assessment prompt"
      contains: "risk, cost"
    - path: "references/prompts/compare.md"
      provides: "Conflict detection prompt"
      contains: "conflict, compare"
  key_links: []
---

<objective>
Create test infrastructure and template files for Phase 3 PRD Generation.

Purpose: Provide test scaffolds for all PRD requirements and create PRD template/prompt files that downstream plans will implement against.
Output: Test file with all test classes as stubs, 4 Jinja2 PRD templates, 4 prompt templates.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-prd-generation/03-RESEARCH.md
@.planning/phases/03-prd-generation/03-VALIDATION.md

# Existing patterns from codebase
@scripts/compiler.py (LLM integration pattern)
@scripts/search.py (FTS5 search pattern)
@references/prompts/compile.md (prompt template pattern)
@tests/conftest.py (fixture pattern)

<interfaces>
<!-- Key interfaces executor needs. Extracted from codebase. -->

From scripts/search.py:
```python
def search_wiki(
    db_path: Path,
    query: str,
    scope: str | None = None,
    limit: int = 20,
) -> list[dict]:
    """Search the wiki using FTS5 with BM25 ranking."""
```

From scripts/compiler.py:
```python
def get_anthropic_client():
    """Get Anthropic client with API key from environment."""

def load_compile_prompt() -> str:
    """Load the compilation prompt template."""
```

From scripts/config.py:
```python
class PMSkillConfig(BaseModel):
    raw_dir: Path
    wiki_dir: Path
    log_dir: Path
    llm: LLMConfig
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test scaffold for PRD module</name>
  <files>tests/test_prd.py</files>
  <action>Create tests/test_prd.py with test class stubs for all PRD requirements:

- TestConversation (PRD-01): Test multi-round conversation state management
- TestSuggestion (PRD-02): Test auto-suggestion of concepts/features
- TestRetrieval (PRD-03): Test keyword-based search integration
- TestIterativeRetrieval (PRD-04): Test iterative retrieval loop with convergence
- TestConflictDetection (PRD-05): Test conflict detection between user input and KB
- TestRiskAssessment (PRD-06): Test risk/cost assessment output
- TestPRDStructure (PRD-07): Test PRD follows industry standard structure
- TestTemplates (PRD-08-11): Test all 4 template types render correctly

Each test class should have placeholder test methods marked with pytest.mark.skip(reason="Wave 0 scaffold"). Include imports for pytest, unittest.mock, and scripts.prd module (which will be created later).

Do NOT implement test logic - only create the structure with skip markers so Wave 1+ plans can implement.</action>
  <verify>
    <automated>pytest tests/test_prd.py --collect-only 2>&1 | grep -c "test_" | grep -q "8"</automated>
  </verify>
  <done>tests/test_prd.py exists with 8 test classes, each with at least one test method stub, all marked skip</done>
</task>

<task type="auto">
  <name>Task 2: Create PRD Jinja2 templates</name>
  <files>references/templates/toc-prd.md.jinja2, references/templates/tob-prd.md.jinja2, references/templates/backend-prd.md.jinja2, references/templates/miniprogram-prd.md.jinja2</files>
  <action>Create 4 PRD templates as Jinja2 files in references/templates/:

1. toc-prd.md.jinja2 (ToC consumer product):
   - Sections: Background, User Stories, User Flows, Error Handling, Analytics Events, References
   - Focus on UX, flows, error states, data tracking

2. tob-prd.md.jinja2 (ToB business product):
   - Sections: Background, Business Logic, Permissions & Roles, Approval Workflows, Delivery & Acceptance, References
   - Focus on business rules, permissions, approvals

3. backend-prd.md.jinja2 (Backend/admin system):
   - Sections: Background, List Pages, CRUD Operations, Permissions, Audit Logs, Performance, References
   - Focus on lists, permissions, operation logs

4. miniprogram-prd.md.jinja2 (Mini-program/WeChat):
   - Sections: Background, User Scenarios, Platform Rules, Authorization Flow, Performance Constraints, References
   - Focus on scenarios, platform compliance, performance

Each template should include Jinja2 variable placeholders ({{ title }}, {% for story in user_stories %}) and a References section for citations. Follow pattern from RESEARCH.md template examples.</action>
  <verify>
    <automated>ls references/templates/*.jinja2 | wc -l | grep -q "4"</automated>
  </verify>
  <done>All 4 Jinja2 templates exist with proper sections and variable placeholders</done>
</task>

<task type="auto">
  <name>Task 3: Create prompt templates for PRD generation</name>
  <files>references/prompts/collect.md, references/prompts/suggest.md, references/prompts/assess.md, references/prompts/compare.md</files>
  <action>Create 4 prompt templates in references/prompts/ following the compile.md pattern:

1. collect.md (Requirement collection):
   - Instructions for multi-round conversation
   - Ask clarifying questions about product type, features, users
   - Extract structured requirements (title, features, constraints)
   - Mark with confidence annotations

2. suggest.md (Keyword suggestion):
   - Given user input and context, extract keywords for knowledge base search
   - Suggest related concepts, features, APIs, data fields
   - Output format: JSON list of keywords

3. assess.md (Risk/cost assessment):
   - Analyze requirements for implementation risks
   - Categories: technical, business, legal
   - Output structured risk assessment with severity levels
   - Estimate effort and complexity

4. compare.md (Conflict detection):
   - Compare user requirements against knowledge base content
   - Identify contradictions or inconsistencies
   - Output conflicts with source citations
   - Suggest resolution options

Each prompt should include: role definition, task description, output format example, confidence annotation instructions.</action>
  <verify>
    <automated>ls references/prompts/*.md | wc -l | grep -q "5"</automated>
  </verify>
  <done>All 4 new prompt templates exist (total 5 with compile.md)</done>
</task>

<task type="auto">
  <name>Task 4: Add PRD fixtures to conftest.py</name>
  <files>tests/conftest.py</files>
  <action>Add new fixtures to tests/conftest.py for PRD testing:

1. mock_conversation_state - Mock RequirementCollection dataclass with sample turns
2. sample_prd_requirements - Dict with typical PRD input (title, features, user_stories)
3. mock_prd_context - Dict with compiled wiki context for retrieval
4. mock_search_results - List of mock FTS5 search results with snippets
5. mock_risk_assessment - Dict with sample risk/cost assessment output

Follow existing fixture patterns in conftest.py. Each fixture should return realistic test data that matches expected PRD module inputs/outputs.</action>
  <verify>
    <automated>grep -c "prd" tests/conftest.py | grep -q "5"</automated>
  </verify>
  <done>5 new PRD fixtures added to conftest.py</done>
</task>

</tasks>

<verification>
All test scaffolds created, all templates exist, prompts ready for Wave 1+ implementation.
</verification>

<success_criteria>
- tests/test_prd.py has 8 test class stubs
- 4 Jinja2 templates in references/templates/
- 4 prompt templates in references/prompts/
- 5 PRD fixtures in tests/conftest.py
- pytest --collect-only shows all test classes
</success_criteria>

<output>
After completion, create `.planning/phases/03-prd-generation/03-01-SUMMARY.md`
</output>